__pycache__/
*.pyc
.DS_Store

# Runtime logs/screenshots/reports are regenerated every run and are noisy
# in diffs -- but these two state files need to persist across runs
# (disabled->enabled / marquee-text-change detection), so keep them.
logs/*
!logs/last_state.json
!logs/last_marquee.json
